<header>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Roopvilla Project</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <ul class="navbar-nav me-auto mb-2 mb-md-0">
            <li class="nav-item">
              <a class="nav-link <%= path === 'home' ? 'active' : '' %>" <%= path === 'home' ? 'aria-current="page"' : '' %>  href="/home">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= path === 'monthly' ? 'active' : '' %>" <%= path === 'monthly' ? 'aria-current="page"' : '' %> href="/monthly">Monthly Data</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= path === 'stats' ? 'active' : '' %>" <%= path === 'stats' ? 'aria-current="page"' : '' %> href="/stats">Statistics</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= path === 'mailer' ? 'active' : '' %>" <%= path === 'mailer' ? 'aria-current="page"' : '' %> href="/mailer">Mailer</a>
            </li>
            <li class="nav-item">
                <a class="nav-link <%= path === 'tech' ? 'active' : '' %>" <%= path === 'tech' ? 'aria-current="page"' : '' %> href="/tech">Tech Stack</a>
            </li>
          </ul>
          <div class="d-flex">
            <span class="nav-link nav-item" style="font-weight: bold;">Welcome, <%= user.OWNER_NAME%></span>
            <button id="logout" class="btn btn-outline-success nav-item">Logout</button>
          </div>
        </div>
      </div>
    </nav>
  </header>